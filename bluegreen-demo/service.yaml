apiVersion: v1
kind: Service

metadata:
  name: apache-active

spec:
  selector:
    app: apache

  ports:
  - port: 80
    targetPort: 80

---
apiVersion: v1
kind: Service

metadata:
  name: apache-preview

spec:
  selector:
    app: apache

  ports:
  - port: 80
    targetPort: 80
